# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool
*.out

# Go workspace file
go.work
go.work.sum

# Dependency directories
vendor/

# Build output
bin/
dist/

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Job runtime data (never commit job state)
jobs/

*.tar.gz

# Test coverage
coverage.txt
coverage.html

# Temporary files
*.tmp
*.log

# Downloaded test data (downloaded in CI, not committed)
.github/test/torch/testdata/

# Node.js dependencies (installed via npm install)
node_modules/
package-lock.json

# Node.js cache
.npm/
.cache/

# VitePress build output and cache
docs/.vitepress/dist/
docs/.vitepress/cache/

# Patch coverage
coverage.xml
patch-coverage.html

# Vale style packages (downloaded via `vale sync`; config and custom rules are committed)
.vale/styles/*
!.vale/styles/config/
!.vale/styles/aether/
